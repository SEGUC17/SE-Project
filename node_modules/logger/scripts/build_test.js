const rollup = require('rollup').rollup;

const bundle = (src, dest) => {
  return rollup({
    entry: src,
  })
  .then((bundled) => {
    bundled.write({
      format: 'iife',
      useStrict: false,
      dest,
    });
  });
};

bundle('test/test.js', 'test/test.compiled.js');
